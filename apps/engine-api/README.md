# apps/engine-api

Reserved for the Python engine service.

This app should wrap the current checkers engine code and expose a narrow API for:

- validating moves
- generating bot replies
- loading checkpoints
- reporting search/eval metadata

No implementation scaffold added yet.

